1. Path Traversal Vulnerability in File Downloads 
The knowledge module's download endpoint does not validate that file paths are within the storage directory, allowing potential access to arbitrary files on the filesystem.

#### C1. Path Traversal in File Download Endpoint
**File:** `/Users/Berzins/Desktop/VTV/app/knowledge/routes.py`, lines 147-161
**Standard:** Security
The download endpoint resolves the file path from the database and serves it via `FileResponse`. However, if an attacker can inject a crafted `file_path` into the database (e.g., via a compromised import, direct DB access, or a bug in the storage logic), `.resolve()` does not prevent serving arbitrary files from the filesystem.
```python
@router.get("/documents/{document_id}/download")
async def download_document(...) -> FileResponse:
    file_path, filename = await service.get_document_file_path(document_id)
    return FileResponse(
        path=Path(file_path).resolve(),  # No validation against storage root
        filename=filename,
        media_type="application/octet-stream",
    )
```
**Fix:** Validate that the resolved path starts with the configured `document_storage_path` before serving:
```python
resolved = Path(file_path).resolve()
storage_root = Path(settings.document_storage_path).resolve()
if not str(resolved).startswith(str(storage_root)):
    raise ProcessingError("File path outside storage directory")



) -> DocumentResponse:
    """Update document metadata (title, description, domain, language)."""
    _ = request
    return await service.update_document(document_id, body)
@router.get("/documents/{document_id}/download")
@limiter.limit("30/minute")
async def download_document(
    request: Request,
    document_id: int,
    service: KnowledgeService = Depends(get_service),  # noqa: B008
) -> FileResponse:
    """Download the original uploaded file."""
    _ = request
```

2. No File Size Limits on Uploads 
The document upload endpoint reads entire files into memory without size validation, creating a memory exhaustion risk.

#### C2. No File Size Limit on Upload
**File:** `/Users/Berzins/Desktop/VTV/app/knowledge/routes.py`, lines 65-105
**Standard:** Security
The `upload_document` endpoint reads the entire file into memory (`content = await file.read()`) with no size check. The project has a `BodySizeLimitMiddleware` at 100KB, but typical document uploads (PDFs, Excel files) will commonly exceed this. Either the middleware blocks legitimate uploads, or if the limit is raised, an attacker can exhaust server memory.
```python
content = await file.read()  # No streaming, no size check
tmp.write(content)
```
**Fix:** Add explicit file size validation in the route, and stream to disk in chunks rather than reading the entire file into memory:
```python
MAX_UPLOAD_SIZE = 50 * 1024 * 1024  # 50MB
content = await file.read()
if len(content) > MAX_UPLOAD_SIZE:
    raise HTTPException(status_code=413, detail="File too large")
```
Or better, stream in chunks to avoid memory pressure.

### 3. **Filename Injection Vulnerability**  
User-provided filenames are used directly without sanitization, allowing path traversal attacks like `../../etc/passwd`. [0-cite-4](#0-cite-4) [0-cite-5](#0-cite-5)   
  
#### C3. Filename Injection via Unsanitized Filename
**File:** `/Users/Berzins/Desktop/VTV/app/knowledge/routes.py`, line 89; `/Users/Berzins/Desktop/VTV/app/knowledge/service.py`, lines 124-126
**Standard:** Security
The original filename from the upload is used directly to construct a storage path with no sanitization:
```python
# routes.py
suffix = Path(file.filename or "upload").suffix  # Trusts user input
# service.py
stored_path = storage_dir / filename  # filename comes from user
shutil.copy2(file_path, stored_path)
```
A filename like `../../etc/passwd` or `foo\x00.pdf` could cause path traversal or null-byte injection.
**Fix:** Sanitize the filename before use:
```python
import re
safe_name = re.sub(r'[^\w\-.]', '_', Path(filename).name)
stored_path = storage_dir / safe_name

            storage_dir.mkdir(parents=True, exist_ok=True)
            stored_path = storage_dir / filename
            shutil.copy2(file_path, stored_path)

### 4. **Hardcoded Demo Credentials**  
The frontend authentication has hardcoded demo credentials (`admin@vtv.lv` / `admin`) that would be a severe security risk if deployed to production. [0-cite-6](#0-cite-6) [0-cite-7](#0-cite-7)   
  
| fe/C1 | frontend | Hardcoded demo credentials in `auth.ts` (`admin@vtv.lv` / `admin`) |

        demo_users = [
            ("admin@vtv.lv", "admin", "VTV Admin", "admin"),
            ("dispatcher@vtv.lv", "admin", "VTV Dispatcher", "dispatcher"),
            ("editor@vtv.lv", "admin", "VTV Editor", "editor"),
            ("viewer@vtv.lv", "admin", "VTV Viewer", "viewer"),
(********* WE need to create actual users ****************)

### 5. **SQL ILIKE Wildcard Injection**  
Search parameters don't escape SQL wildcards (`%` and `_`), allowing users to manipulate query patterns. [0-cite-8](#0-cite-8) [0-cite-9](#0-cite-9)   
  
## High Severity Issues  
  
            query = query.where(Stop.stop_name.ilike(f"%{search}%"))

### 1. ILIKE Wildcard Injection (3 occurrences)
- `stops/repository.py`, `schedules/repository.py`, `core/agents/tools/`
- User `%` and `_` not escaped in search parameters
- **Fix:** Escape wildcards before ILIKE queries

### 6. **No Authentication/Authorization on API Endpoints**  
The backend API endpoints have **no authentication layer**. Anyone can call the agent endpoints, transit APIs, and other services without credentials. The login endpoint exists but doesn't issue tokens, and no endpoints check for authentication. [0-cite-10](#0-cite-10) [0-cite-11](#0-cite-11)   
  
The authentication only exists on the frontend via Auth.js, but the backend APIs are completely open.  
  

@router.post("/chat/completions", response_model=ChatCompletionResponse)
@limiter.limit("10/minute")
async def chat_completions(
    request: Request,
    body: ChatCompletionRequest,
    service: AgentService = Depends(get_agent_service),
) -> ChatCompletionResponse:
    """Create a chat completion.
    Accepts a list of messages and returns the agent's response
    in OpenAI-compatible format.
    Args:
        request: The incoming HTTP request (used for rate limiting).
        body: Chat completion request with messages.
        service: Agent service instance (injected).
    Returns:
        Chat completion response with the agent's message.
    """
    # Check daily quota before expensive LLM call
    client_ip = request.client.host if request.client else "unknown"
    tracker = get_quota_tracker()
    if not tracker.check_and_increment(client_ip):
        remaining = tracker.get_remaining(client_ip)
        raise HTTPException(
            status_code=429,
            detail=f"Daily query quota exceeded. Remaining: {remaining}. Resets in 24 hours.",
        )
    return await service.chat(body)

@router.post("/login", response_model=LoginResponse)
@limiter.limit("10/minute")
async def login(
    request: Request,
    body: LoginRequest,
    service: AuthService = Depends(get_service),  # noqa: B008
) -> LoginResponse:
    """Authenticate user with email and password."""
    _ = request
    return await service.authenticate(body.email, body.password)


### 7. **Rate Limiter Bypass via X-Forwarded-For Spoofing**  
The rate limiter extracts client IP from the `X-Forwarded-For` header without validation, allowing attackers to bypass rate limits by spoofing this header. [0-cite-12](#0-cite-12) [0-cite-13](#0-cite-13)   
  
    """Extract client IP from request for rate limiting.
    Uses X-Forwarded-For header if present (behind reverse proxy),
    falls back to direct client address.
    Args:
        request: The incoming Starlette request.
    Returns:
        Client IP address string.
    """
    forwarded = request.headers.get("X-Forwarded-For")
    if forwarded:
        return forwarded.split(",")[0].strip()

| core/H4 | core | X-Forwarded-For header spoofing bypasses rate limiter |


### 8. **Redis Credentials Leaked in Logs**  
Database connection URLs containing credentials may be logged, exposing sensitive information. [0-cite-14](#0-cite-14)   
  
| core/H2 | core | Redis URL with credentials leaked to logs |


### 9. **In-Memory Brute Force Protection (Non-Persistent)**  
The brute force protection uses in-memory storage that resets on server restart, allowing attackers to bypass lockouts. [0-cite-15](#0-cite-15) [0-cite-16](#0-cite-16)   
  
# Brute-force protection constants
MAX_FAILED_ATTEMPTS = 5
LOCKOUT_DURATION = datetime.timedelta(minutes=15)

// SECURITY: Frontend brute-force protection (defense in depth — backend also enforces)
const LOGIN_MAX_ATTEMPTS = 5;
const LOGIN_LOCKOUT_MS = 15 * 60 * 1000; // 15 minutes
interface LoginAttempt {
  count: number;
  lockedUntil: number | null;
}

### 10. **No HTTPS/TLS Configuration**  
The production nginx configuration only exposes port 80 (HTTP) and references certificates but doesn't configure SSL. [0-cite-17](#0-cite-17) [0-cite-18](#0-cite-18)   
  
## Medium Severity Issues  
  
    server {
        listen 80;
        server_name _;

  nginx:
    ports:
      - "443:443"
      - "80:80"
    volumes:
      - ./nginx/certs:/etc/nginx/certs:ro

### 11. **Database Credentials Hardcoded in Docker Compose**  
Development Docker configuration uses hardcoded credentials (`postgres:postgres`) that override `.env` settings. [0-cite-19](#0-cite-19) [0-cite-20](#0-cite-20)   
  

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: vtv_db

| `docker-compose.yml:50` | 8. Security | `migrate` service hardcodes `DATABASE_URL` with plaintext credentials `postgres:postgres`. If `.env` provides a different password, the migration will connect with wrong creds. | Remove the hardcoded `DATABASE_URL` from `migrate.environment` and rely on `.env` file only, or use `${POSTGRES_PASSWORD:-postgres}` interpolation to stay consistent with the `db` service. | Medium |
| `docker-compose.yml:90` | 8. Security | `app` service also hardcodes `DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/vtv_db` which overrides any `.env` value. | Same fix — use `DATABASE_URL=${DATABASE_URL:-postgresql+asyncpg://postgres:postgres@db:5432/vtv_db}` or remove the override and document that `.env` must set Docker-internal URLs. | Medium |

### 12. **Missing Input Validation**  
Various endpoints lack proper validation on user inputs like feed_id, route_id, and other parameters.  
  
### 13. **Exposed Database Ports**  
The development configuration exposes PostgreSQL (5433) and Redis (6379) ports directly, which should be removed in production. [0-cite-21](#0-cite-21) [0-cite-22](#0-cite-22)   
  
## Positive Security Features  
  
The application does implement several good security practices:  
  
- **Bcrypt password hashing** with proper salting [0-cite-23](#0-cite-23)   
  
- **Rate limiting** on critical endpoints (though bypassable) [0-cite-24](#0-cite-24)   
  
- **Security headers** in nginx (X-Content-Type-Options, X-Frame-Options, etc.) [0-cite-25](#0-cite-25)   
  
- **Non-root Docker user** for application container [0-cite-26](#0-cite-26)   
  
- **Body size limits** via middleware [0-cite-27](#0-cite-27)   
  
- **SQLAlchemy with parameterized queries** (prevents traditional SQL injection) [0-cite-28](#0-cite-28)   
  
- **Request ID tracking** for security audit logging [0-cite-29](#0-cite-29)   
  
## Recommended Fix Priority  
  
**Before ANY production deployment:**  
1. Fix path traversal in knowledge download (C1)  
2. Add file size validation and sanitize filenames (C2, C3)  
3. Remove hardcoded demo credentials (C1)  
4. Implement proper authentication/authorization on backend APIs  
5. Add HTTPS/TLS configuration  
  
**Short-term (production hardening):**  
6. Escape ILIKE wildcards in all search queries  
7. Fix rate limiter X-Forwarded-For spoofing  
8. Move brute force tracking to Redis (persistent)  
9. Sanitize Redis URL in logs  
10. Remove exposed database ports in production  
  
**Medium-term:**  
11. Add comprehensive input validation across all endpoints  
12. Implement proper RBAC on backend routes  
13. Add security monitoring and alerting  
14. Conduct penetration testing  
  
## Notes

The codebase shows good engineering practices overall (structured logging, type safety, testing), but the security layer is incomplete. The frontend has RBAC via Auth.js, but the backend APIs are completely open with no authentication checks. This is the **most critical architectural gap** - all backend endpoints can be called without any credentials.

## Remediation Status (2026-02-22, commit 85bf32d)

| # | Finding | Severity | Status | Fix Applied |
|---|---------|----------|--------|-------------|
| C1 | Path traversal in download | Critical | FIXED | `is_relative_to()` validation in `knowledge/routes.py` (pre-existing fix) |
| C2 | No file size limit on upload | Critical | FIXED | Streaming chunked reads (8KB) with 50MB limit, `HTTPException(413)` |
| C3 | Filename injection | Critical | FIXED | `re.sub(r"[^\w\-.]", "_", ...)` + `is_relative_to()` path validation |
| 4 | Hardcoded demo credentials | High | FIXED | Environment-controlled: `ENVIRONMENT != development` skips seeding, `DEMO_USER_PASSWORD` configurable |
| 5 | ILIKE wildcard injection | High | FIXED | `escape_like()` in `app/shared/utils.py`, applied to stops/schedules/drivers repos |
| 6 | No backend API auth | Critical | DEFERRED | Architectural change (~2-3 days), tracked in `docs/TODO.md` |
| 7 | Rate limiter X-Forwarded-For spoofing | Medium | FIXED | Switched to `X-Real-IP` header (nginx-set, not spoofable) |
| 8 | Redis credentials in logs | Medium | FIXED | `_redact_url()` in `app/core/redis.py` masks passwords before logging |
| 9 | In-memory brute force (non-persistent) | Medium | DEFERRED | Requires Redis-backed tracking, tracked in `docs/TODO.md` |
| 10 | No HTTPS/TLS | Medium | PARTIAL | Commented HTTPS template added to `nginx/nginx.conf`, requires cert provisioning |
| 11 | Docker credentials hardcoded | Medium | FIXED | `${POSTGRES_USER:-postgres}` / `${POSTGRES_PASSWORD:-postgres}` interpolation |
| 12 | Missing input validation | Medium | FIXED | `max_length` + `pattern` constraints on transit query params |
| 13 | Exposed database ports | Low | DOCUMENTED | Already has `# SECURITY` comment, production concern only |

**Regression tests:** 33 security-specific tests in `app/tests/test_security.py` covering all fixed findings.
**Anti-patterns:** Security rules 40-45 added to `docs/python-anti-patterns.md`.
**Slash commands:** `/review` expanded to 10 security checks, `/be-planning` and `/be-execute` updated with security pitfalls 41-46.  
  




































